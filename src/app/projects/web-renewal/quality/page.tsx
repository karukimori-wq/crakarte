const checks=[{name:"PC表示",items:["主要ブラウザでレイアウト崩れがない","リンク切れがない","画像が適切に表示される"],done:3},{name:"スマートフォン",items:["主要画面が適切に表示される","文字サイズが読みやすい","ボタンが押しやすい"],done:2},{name:"コンテンツ",items:["誤字脱字がない","クライアント確定内容と一致"],done:2}];
export default function Quality(){return <main className="standalone"><a className="back" href="/projects/web-renewal">← Webサイトリニューアル</a><header className="page-head"><div><p className="eyebrow">QUALITY CHECK</p><h1>品質確認</h1><p>納品前の確認項目を検査証のように記録し、抜け漏れを防ぎます。</p></div><button className="primary">検査を完了する</button></header><section className="quality-summary"><div><span>確認済み</span><strong>7 / 8</strong></div><div><span>要修正</span><strong>1</strong></div><div><span>進捗</span><strong>88%</strong></div></section>{checks.map((c,gi)=><section className="panel quality-group" key={c.name}><div className="section-head"><h2>{c.name}</h2><span className="count">{c.done} / {c.items.length}</span></div>{c.items.map((item,i)=>{const issue=gi===1&&i===2;return <div className="quality-row" key={item}><span className={issue?"quality-issue":"quality-ok"}>{issue?"!":"✓"}</span><div className="grow"><strong>{item}</strong>{issue&&<span>CTAボタンのタップ領域を拡大する</span>}</div><b>{issue?"要修正":"OK"}</b></div>})}</section>)}</main>}
